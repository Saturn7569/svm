;;;;;;;;;;;;;;;
;             ;
; Count to 10 ;
;             ;
;;;;;;;;;;;;;;;

; Create the counter
ICONST 0
;Labels are executed when they are declared (labels declared after HLT will not execute when declared)
::loop
    ; Add 1 to the counter
    ICONST 1
    ADD

    DPRINT

    ; Check the value
    DUP
    ICONST 10
    EQ

    ; Continue looping if the value is not 10
    JNZ ::loop
    ; JNZ will execute the ::loop label if the top value is 0
HLT